---
name: Remove dead query keys and useQuery calls
status: open
created: 2026-05-05T00:36:38Z
updated: 2026-05-05T00:36:38Z
github: (will be set on sync)
depends_on: ['001']
parallel: false
conflicts_with: ['003']
---

# Task: Remove dead query keys and useQuery calls

## Description

Delete stale query key entries from `queryKeys.ts` for live ESI resources that are now managed by Zustand. Remove any leftover `useQuery` calls in migrated hooks that still reference these keys.

## Acceptance Criteria

- [ ] `queryKeys.ts` contains no keys for `queue`, `skills`, `locations`, `attributes`, `clones`, `remaps`
- [ ] No `useQuery` calls remain in migrated live-data hooks
- [ ] TypeScript compiles cleanly after removal

## Technical Details

Target keys to remove from `src/hooks/tauri/queryKeys.ts`:

- `skill_queue` / `queue`
- `character_skills` / `skills`
- `locations_overview` / `locations`
- `attributes`
- `clones`
- `remaps`

Also remove any hook files or hook functions that were `useQuery`-based wrappers for these live resources if they weren't cleaned up in Phase 2.

## Dependencies

Requires audit task (001) to confirm exact hit list.

## Effort Estimate

- Size: S
- Hours: 2

## Definition of Done

- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
