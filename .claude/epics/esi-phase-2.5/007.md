---
name: Remove deleted Rust commands + final audit
status: open
created: 2026-05-07T04:52:20Z
updated: 2026-05-07T04:52:20Z
github: (will be set on sync)
depends_on: [005, 006]
parallel: false
conflicts_with: []
---

# Task: Remove deleted Rust commands + final audit

## Context

Tasks 005/006 remove frontend callsites. This task removes Rust impls + final audit to verify data layer clean end-to-end.

## Work

### Delete obsolete command handlers

Remove from `src-tauri/src/commands/`:

`skill_queues.rs`: remove `get_skill_queue_for_character`, `get_skill_queues`, `get_training_characters_count`. Remove `force_refresh_skill_queue` **only if** superseded — keep if still used as manual refresh trigger.

`skills.rs`: remove `get_character_skills_with_groups` (or whole file if nothing else remains).

`attributes.rs`: remove `get_character_attributes_breakdown` (or whole file if nothing else remains).

`clones.rs`: remove `get_clones` (or whole file if nothing else remains).

`location.rs`: remove `get_all_characters_locations` and `get_character_location`. File likely gone entirely.

`overview.rs`: remove `get_training_characters_overview` — delete entire file.

### `src-tauri/src/commands/mod.rs`

Remove all registrations + `pub use` re-exports for deleted commands. Update `tauri::generate_handler![]` macro to exclude removed commands.

### `src-tauri/src/lib.rs` or `main.rs`

Verify no deleted command names in handler registration.

### Unused imports / dead code

Run `pnpm lint:rust` — fix unused import warnings from deletions. Remove unused helpers only called by deleted commands.

### Final audit checklist

**Backend:**

- [ ] `refresh/mod.rs` — every `Ok(None)` arm emits re-hydration event (no silent no-ops).
- [ ] All surviving command types annotated with `#[typeshare]`.
- [ ] No tauri-typegen refs in `Cargo.toml`, `build.rs`, `tauri.conf.json`, or any config.
- [ ] `pnpm lint:rust` clean.
- [ ] `cargo build` succeeds.

**Frontend:**

- [ ] `src/generated/commands.ts` does not exist.
- [ ] No file imports from `@/generated/commands`.
- [ ] No `invoke()` call uses command name no longer in Rust.
- [ ] `esiEvents.ts` listeners synchronous — no `async`, no `await`, no command calls.
- [ ] `__root.tsx` hydration calls `get_esi_snapshot` exactly once, no character ID arg.
- [ ] No `useMemo` in components performs ESI data joins across store slices.
- [ ] `useTrainingCharactersOverview` hook does not exist.
- [ ] `pnpm typecheck` passes.
- [ ] `pnpm lint` passes.
- [ ] `pnpm verify` passes end-to-end.

**Manual smoke test:**

- [ ] Launch app warm SQLite cache + ESI network disabled → all screens show data, no spinners.
- [ ] Wait for background refresh cycle → UI updates without user action.
- [ ] Force refresh (manual trigger) → data updates correctly.
- [ ] Add new character → supervisor spawns refresher, events flow.
- [ ] Remove character → supervisor cancels refresher, store clears that character's slices.

## Acceptance Criteria

- All audit checklist items checked.
- `pnpm verify` passes.
- App loads with data on launch (warm cache, no network).
- No permanent loading spinners under normal conditions.
