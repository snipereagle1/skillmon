---
name: Update hooks — inline invoke calls, remove dead hooks, fix useOverview pattern
status: closed
created: 2026-05-07T04:52:20Z
updated: 2026-05-07T18:49:00Z
github: (will be set on sync)
depends_on: [004, 005]
parallel: false
conflicts_with: []
---

# Task: Update hooks — inline invoke calls, remove dead hooks, fix useOverview pattern

## Context

`commands.ts` deleted. ESI live-data hooks read Zustand store (populated by events/snapshot). Dead hooks: wrapped deleted commands. Remaining command hooks: move `invoke()` inline. `useOverview` re-fetch-on-queue-change gone — overview data from queue slice directly.

## Work

### Remove dead hooks

Delete hooks wrapping deleted ESI commands, now replaced by store reads:

- `src/hooks/tauri/useCharacterSkills.ts` — store read, still fine; update type to new `SkillsPayload` shape.
- `src/hooks/tauri/useAttributes.ts` — store read; update type.
- `src/hooks/tauri/useClones.ts` — store read; update type.
- `src/hooks/tauri/useLocationsOverview.ts` — store read; update type.
- `src/hooks/tauri/useSkillQueue.ts` — store read; update type.
- `src/hooks/tauri/useOverview.ts` — **delete**. `getTrainingCharactersOverview` gone. Components using `useTrainingCharactersOverview` read queue store slice directly.
- `src/hooks/tauri/useForceRefreshSkillQueue.ts` — **keep**; update to inline `invoke('force_refresh_skill_queue', { characterId })` instead of deleted command wrapper.

### Update store-reading hooks

Update return types to match new enriched payload types (from `@/generated/types`):

- `useSkillQueue` → returns `QueuePayload | null`
- `useCharacterSkills` → returns `SkillsPayload | null`
- `useAttributes` → returns `AttributesPayload | null` (AttributeBreakdown per attribute now in payload)
- `useClones` → returns `ClonePayload | null`
- `useLocationsOverview` → returns `LocationPayload[]`

### Move inline invoke calls

All surviving command hooks that imported from `commands.ts`:

- Replace `import { commandName } from '@/generated/commands'` with `import { invoke } from '@tauri-apps/api/core'`.
- Replace `commandName(params)` with `invoke<ReturnType>('command_name', params)`.
- Import return type from `@/generated/types`.

Hooks needing treatment:

- `useAccountsAndCharacters.ts` — `getAccountsAndCharacters` → inline invoke.
- `useNotifications.ts` — `getNotifications` → inline invoke.
- `useNotificationSettings.ts` — `getNotificationSettings`, `upsertNotificationSetting` → inline invoke.
- `useDismissNotification.ts` — `dismissNotification` → inline invoke.
- `useRemaps.ts` — `getCharacterRemaps`, `getPlanRemaps`, `saveRemap`, `deleteRemap` → inline invoke. Remove `getCharacterAttributesBreakdown` re-fetch in `useSaveRemap`/`useDeleteRemap` mutation callbacks — attributes event fires after supervisor's next cycle. If immediate reflection needed, mutation callback can invoke `get_esi_snapshot` for that character (or accept short delay).
- `useSkillPlans.ts` — inline all plan command invocations.
- `useSettings.ts` — inline settings commands.
- `useSdeSkills.ts` — inline SDE commands.
- `useAuthEvents.ts` — inline auth commands; remove `getCharacterAttributesBreakdown` re-fetch (same reasoning as remaps).
- `useLogoutCharacter.ts` — inline.
- `useStartEveLogin.ts` — inline.
- `useStartupState.ts` — inline.
- `useOptimization.ts`, `usePlanComparison.ts`, `usePlanComparisonAll.ts`, `useSimulation.ts`, `useSkillDetails.ts` — inline all plan/simulation commands.

### Fix direct component imports from commands.ts

Components importing directly from `@/generated/commands` — all must update before `commands.ts` deleted:

- **`src/components/Clones/index.tsx`** — imports `getTypeNames`. Move `invoke<TypeNameEntry[]>('get_type_names', { typeIds })` inline or extract `useTypeNames` hook if multi-use.
- **`src/components/SkillPlans/SimulationTimeline.tsx`** — imports `getTypeNames`. Same treatment.
- **`src/components/SkillPlans/PlanEditor.tsx`** — imports `exportSkillPlanText`, `exportSkillPlanXml`. Move inline into export handler callbacks.
- **`src/components/LocationDemo.tsx`** — imports `getCharacterLocation`. Command deleted; assess: live component or leftover. If live, replace with location store slice; if dead, delete file.

### Fix OverviewTable / AccountsWithoutTraining

`OverviewTable` used `useTrainingCharactersOverview` (deleted). Update to read queue store directly:

- `useEsiStore(state => state.queues)` → derive training characters from queue slices.
- Fields from old `TrainingCharacterOverview` (`account_name`, `current_skill_name`, `sp_per_hour`, `has_implants`, `has_booster`) now in `QueuePayload` (task 001). Update `OverviewTableRow` to new field names.

### Fix SimulationTab

`SimulationTab` uses `useAttributes` to seed simulation profile's initial implant/remap values. New `AttributesPayload` shape has breakdown per attribute — update seeding logic:

- `characterAttributes.charisma.implants` → same path if breakdown struct preserved, else adjust to new shape.
- Verify simulation seeds correctly after type changes.

### Fix AccountSidebar

Remove `useMemo` joining `allCharacters` with `queues`. Queue slice now carries `character_name` and `is_paused` directly. Sidebar reads `useEsiStore(state => state.queues)`, indexes by character ID — no `allCharacters` join needed.

## Acceptance Criteria

- No file in `src/hooks/tauri/` imports from `@/generated/commands` (file deleted).
- `useTrainingCharactersOverview` and `useOverview.ts` gone.
- All surviving hooks import `invoke` from `@tauri-apps/api/core` and types from `@/generated/types`.
- `OverviewTable` derives training characters from queue store, no command re-fetch.
- `AccountSidebar` has no cross-slice `useMemo` join.
- `SimulationTab` seeds correctly from new attributes payload shape.
- `pnpm typecheck` passes.
- `pnpm lint` passes.
