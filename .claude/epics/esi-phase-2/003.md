---
name: Migrate Character Skills Hook to Zustand
status: open
created: 2026-05-05T00:33:29Z
updated: 2026-05-05T00:33:29Z
github: (will be set on sync)
depends_on: [002]
parallel: false
conflicts_with: [002, 004, 005]
---

# Task: Migrate Character Skills Hook to Zustand

## Description

Migrate `useCharacterSkills` from React Query to the Zustand ESI store. Update the Phase 1 event listener for `character:{id}:skills` in `src/lib/esiEvents.ts` to call the store setter. Remove the RQ adapter for skills.

Must run after task 002 (sequential constraint on esiEvents.ts to prevent dual-system violations).

## Acceptance Criteria

- [ ] `useCharacterSkills(characterId)` reads from `useEsiStore(state => state.skills[characterId]?.data ?? null)` — no `useQuery` call
- [ ] Return shape compatible with prior RQ version
- [ ] `esiEvents.ts` skills listener calls `useEsiStore.getState().setSkills(...)` — no `queryClient.setQueryData` for skills
- [ ] Phase 1 RQ adapter for skills removed (no `queryKeys.characterSkills` in event bootstrap)
- [ ] Consumer components unchanged
- [ ] `pnpm typecheck` passes, `pnpm test` passes

## Technical Details

`useCharacterSkills` returns `CharacterSkillsResponse`. Consumers currently destructure `{ data, isLoading, error }`.

Verify call sites: `grep -r "useCharacterSkills" src/` before and after.

Note: `useOverview` (task 006) may depend on skills data — it will be migrated in its own task after this one is complete.

## Dependencies

- Task 001 (store exists)
- Task 002 (queue migration done — sequential constraint enforced)

## Effort Estimate

- Size: S
- Hours: 2

## Definition of Done

- [ ] No `useQuery` in `useCharacterSkills.ts`
- [ ] No `queryClient.setQueryData` for skills in `esiEvents.ts`
- [ ] TypeScript passes
- [ ] Tests pass
- [ ] Code reviewed
