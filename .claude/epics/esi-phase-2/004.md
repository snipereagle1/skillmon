---
name: Migrate Location Hook to Zustand
status: open
created: 2026-05-05T00:33:29Z
updated: 2026-05-05T00:33:29Z
github: (will be set on sync)
depends_on: [003]
parallel: false
conflicts_with: [002, 003, 005]
---

# Task: Migrate Location Hook to Zustand

## Description

Migrate `useAllCharactersLocations` from React Query to the Zustand ESI store. Update the Phase 1 event listener for `character:{id}:location` in `src/lib/esiEvents.ts` to call the store setter. Remove the RQ adapter for location including the hardcoded `refetchInterval: 60_000` and `staleTime: 55_000`.

## Acceptance Criteria

- [ ] `useAllCharactersLocations()` reads all location slices from the store — no `useQuery` call, no `refetchInterval`
- [ ] Return shape: `CharacterLocationOverview[]` (all characters' locations), compatible with prior callers
- [ ] `esiEvents.ts` location listener calls `useEsiStore.getState().setLocation(...)` — no `queryClient.setQueryData` for location
- [ ] Phase 1 RQ adapter for location removed
- [ ] Consumer components unchanged
- [ ] `pnpm typecheck` passes, `pnpm test` passes

## Technical Details

Current `useAllCharactersLocations` returns an array of all characters' locations. The store keyed by `characterId` — the hook must aggregate: `Object.values(state.locations).map(s => s.data).filter(Boolean)`.

Must preserve loading semantics: if no location data loaded yet for any character, treat as loading state.

Verify call sites: `grep -r "useAllCharactersLocations" src/`.

## Dependencies

- Task 001 (store exists)
- Task 003 (skills done — sequential constraint on esiEvents.ts)

## Effort Estimate

- Size: S
- Hours: 2

## Definition of Done

- [ ] No `useQuery` in `useLocationsOverview.ts`
- [ ] No `queryClient.setQueryData` for location in `esiEvents.ts`
- [ ] No `refetchInterval` / `staleTime` for location
- [ ] TypeScript passes
- [ ] Tests pass
- [ ] Code reviewed
